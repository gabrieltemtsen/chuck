{
  "name": "chuck",
  "private": true,
  "version": "0.0.1",
  "description": "Chuck ~ The Intersect — X autopilot + opportunity scout + paper trading, deployed on Nosana",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm -w @chuck/dashboard run dev",
    "build": "npm run build -ws",
    "lint": "npm run lint -ws"
  },
  "engines": {
    "node": ">=20"
  }
}
