{
  "name": "chuck",
  "private": true,
  "version": "0.0.1",
  "description": "Chuck ~ The Intersect — X autopilot + opportunity scout + paper trading, deployed on Nosana",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm -w @chuck/dashboard run dev",
    "build": "npm -w @chuck/shared run build && npm -w @chuck/db run build && npm -w @chuck/agent run build && npm -w @chuck/dashboard run build",
    "lint": "npm run lint -ws"
  },
  "engines": {
    "node": ">=20"
  }
}
